<%= form_for [@post, @comment] do |f| %>
	<%= f.label :body, "Add a comment" %>
	<%= f.text_area :body, :rows => 3, :cols => 200, :style => 'width:500px;' %><br/>
	<%= f.submit "Post Comment", :class => "btn btn-primary" %>
<% end %>